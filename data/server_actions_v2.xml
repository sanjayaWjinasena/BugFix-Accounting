<?xml version="1.0" encoding="utf-8"?>
<!--
    v0.0.20: Placeholder ir.actions.server + ir.actions.act_window
    records for the 11 Clear-DB actions referenced by numeric ID
    (name="NNNN") in views/account_move_studio_ported_v2.xml.

    Why placeholders instead of full ports:
      The 9 Clear-DB server actions carry between 600 and 31,000 bytes
      of Python code each. Porting all of them verbatim in a single
      commit is high-risk (each has different depends chain into
      other Studio artefacts). This commit provides STABLE XMLIDS so
      the view arch can reference them via %(...)d interpolation
      (unblocking install on fresh envs where Clear-DB's auto-assigned
      ids don't match), while the actual code arrives per-action in
      follow-up commits.

    Each stub raises UserError so any operator clicking the button
    immediately sees "action not yet fully ported" instead of a
    silent no-op, making the port backlog visible in the UI.

    Correspondence (Clear-DB id -> local xmlid):

      2176 -> srv_rug_account_update_in_si
      2762 -> srv_update_advance_payment_account_vendor_bill
      1370 -> srv_imp_update_consignment_pi
      2366 -> srv_imp_vendor_bill_currency_rate
      1493 -> srv_sls_request_credit_note_approval
      1495 -> srv_sls_credit_note_approval_main
      1854 -> srv_sls_view_credit_limit_validation
      1851 -> srv_sls_send_bank_guarantee_notification
      1852 -> srv_sls_view_bank_guarantee_validation
      1371 -> act_dispatch_reversal (window action, ported verbatim)
      1372 -> act_custom_clearance_reversal (window action, ported verbatim)
-->
<odoo>

    <!-- ==================================================== -->
    <!-- Server action stubs (9)                              -->
    <!-- ==================================================== -->

    <!-- 2176: RR - RUG Account Update in SI. FULL PORT (v0.0.39).
         When invoice's x_studio_rug_confirmed is True, rewrites all
         account.move.line entries pointing at the journal's default
         account to instead point at x_repair_accounts.x_studio_rug_account
         (per-company), then marks x_studio_rug_acc_updated=True on
         the invoice. Depends on env['x_repair_accounts'] which is
         owned by Fix-repair (installed alongside on repair-test-101).
         No install-time model dep needed - env[] resolution happens at
         click time. -->
    <record id="srv_rug_account_update_in_si" model="ir.actions.server">
        <field name="name">RR - RUG Account Update in SI</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.x_studio_rug_confirmed == True:
    company_id = env.context.get('allowed_company_ids', [env.user.company_id.id])[0]
    company = env['res.company'].browse(company_id)

    rug_account = env['x_repair_accounts'].search([('x_studio_company_id', '=', company.id)], limit=1)
    if rug_account:
        if rug_account.x_studio_rug_account == False:
            raise UserError('RUG Account must be Specified in Repair Accounts')

    lines = env['account.move.line'].search([
        ('move_id', '=', record.id),
        ('account_id', '=', record.journal_id.default_account_id.id),
    ])
    for line in lines:
        line.write({'account_id': rug_account.x_studio_rug_account.id})

    record.write({'x_studio_rug_acc_updated': True})
]]></field>
    </record>

    <!-- 2762: Project - Update Advance Payment Account - Vendor Bill.
         FULL PORT (v0.0.40). Companion to 2739 (already ported for
         account.payment): rewrites either income (out_invoice) or
         expense (in_invoice) journal lines to the per-project advance
         account defined on x_advance_payment_acco singleton (id=1),
         then flags x_studio_advance_acc_updated=True.

         usage='base_automation' on Clear-DB - Studio uses this action
         as an automation trigger, not manual click. We still expose
         via binding_view_types="list,form" to match Clear-DB's
         side-effect surface. -->
    <record id="srv_update_advance_payment_account_vendor_bill" model="ir.actions.server">
        <field name="name">Project - Update Advance Payment Account - Vendor Bill</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">base_automation</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.x_studio_type == 'Advance Payment':

    val = False
    for line in record.invoice_line_ids:
        val = True

    if val == False:
        raise UserError('You need to add a line before updating the advance account.')

    account = env['x_advance_payment_acco'].search([('id', '=', 1)], limit=1)
    if account:
        if account.x_studio_advance_account_project == False:
            raise UserError('Advance Account (Project) must be Specified in Accounting Configuration')

        if record.move_type == 'out_invoice':
            lines = env['account.move.line'].search([
                ('move_id', '=', record.id),
                ('account_internal_group', '=', 'income'),
            ], limit=1)
            if lines:
                for line in lines:
                    line.write({'account_id': account.x_studio_advance_account_project.id})
                    record.write({'x_studio_advance_acc_updated': True})
        elif record.move_type == 'in_invoice':
            lines = env['account.move.line'].search([
                ('move_id', '=', record.id),
                ('account_internal_group', '=', 'expense'),
            ], limit=1)
            if lines:
                for line in lines:
                    line.write({'account_id': account.x_studio_advance_account_project.id})
                    record.write({'x_studio_advance_acc_updated': True})
        else:
            raise UserError('This feature is not supported.')
    else:
        raise UserError('Advance Payment Accounts have not been setup in Accounting Configuration')
]]></field>
    </record>

    <!-- 1370: IMP - Update Consignment - PI. FULL PORT (v0.0.45).
         Byte-verbatim from Clear-DB (~30 KB Python). Reconciles the
         current vendor bill (record = account.move) against a
         consignment referenced by x_studio_consignment_no. For each
         matching invoice line vs. x_consignment_line pair, aggregates
         charge/duty/tax totals across four buckets (billable local,
         non-billable local, billable custom-clearance, non-billable
         custom-clearance) and rewrites invoice lines using ledger
         accounts read from x_imports_ledger_setup. Also creates two
         side journal entries (vendor-despatch reversal + custom-
         clearance reversal) when the consignment header enables them.
         Finally flags x_studio_update_consignment=True.

         Cross-repo dependencies verified on-disk:
           * x_consignment_header / x_consignment_line -> BugFix-Stock
           * x_imports_ledger_setup -> BugFix-Purchase
           * account.move / account.move.line / product.product /
             res.company / res.config.settings / res.currency /
             res.currency.rate / account.journal -> standard Odoo
           * account.move fields x_studio_consignment_no /
             x_studio_purchase_id / x_studio_created_from_vendor_bill /
             x_studio_created_from_vendor_bill_1 / x_studio_update_consignment
             -> models/account_move.py (already declared)
           * account.move.line field x_studio_tags -> models/account_move_line.py
           * v0.0.45 adds models/product_template.py declaring
             x_studio_charge_type (selection Charge/Duty/Tax) and
             x_studio_non_billable (bool) so the three
             product.product.search() calls resolve via _inherits.
    -->
    <record id="srv_imp_update_consignment_pi" model="ir.actions.server">
        <field name="name">IMP - Update Consignment - PI</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[if record.id:
  
  company_id = env.context.get('allowed_company_ids', [env.user.company_id.id])[0]
  company = env['res.company'].browse(company_id)

  con_line = env['x_consignment_line'].search([('x_studio_consignment_header_id', '=', record.x_studio_consignment_no.id), ('x_studio_purchase_id', '=', record.x_studio_purchase_id.id)],limit=1)
  if not con_line:
    raise UserError('The Selected Consignment No is not linked with the Current PO.')
  
  ex_rate = 1

  company_currency = env['res.config.settings'].search([('currency_id', '=', record.currency_id.id)],limit=1)
  if company_currency:
    ex_rate = 1
  else:
    custom_currency = env['res.currency'].search([('id', '=', record.currency_id.id),('active', '=', True)],limit=1)
    if custom_currency:
      custom_currency_rate = env['res.currency.rate'].search([('currency_id', '=', custom_currency.id),('name', '<=', datetime.datetime.today())],order='name desc',limit=1)
      if custom_currency_rate.inverse_company_rate != 0:
        ex_rate = custom_currency_rate.inverse_company_rate
    
    """custom_currency = env['res.currency'].search([('id', '=', record.currency_id.id),('active', '=', True)],limit=1)
    if custom_currency.rate > 0:
      ex_rate = custom_currency.rate"""

  
  done = 0  
  select = 0
  total_amount = 0
  total_charge = 0
  total_duty = 0
  total_tax = 0
  val_charge = False
  val_duty = False
  val_tax = False
  total_charge_cd = 0
  total_duty_cd = 0
  total_tax_cd = 0
  val_charge_cd = False
  val_duty_cd = False
  val_tax_cd = False
  total_charge_ex = 0
  total_duty_ex = 0
  total_tax_ex = 0
  val_charge_ex = False
  val_duty_ex = False
  val_tax_ex = False
  total_charge_ex_cd = 0
  total_duty_ex_cd = 0
  total_tax_ex_cd = 0
  val_charge_ex_cd = False
  val_duty_ex_cd = False
  val_tax_ex_cd = False
  
  tot_charge_ids = []
  tot_duty_ids = []
  tot_tax_ids = []
  tot_final_ids = []
  
  tot_charge_ex_ids = []
  tot_duty_ex_ids = []
  tot_tax_ex_ids = []
  tot_final_ex_ids = []
  
  tot_charge_cd_ids = []
  tot_duty_cd_ids = []
  tot_tax_cd_ids = []
  tot_final_cd_ids = []
  
  tot_charge_ex_cd_ids = []
  tot_duty_ex_cd_ids = []
  tot_tax_ex_cd_ids = []
  tot_final_ex_cd_ids = []
  
  for po_line in record.invoice_line_ids:
    con_line_2 = env['x_consignment_line'].search([('x_studio_consignment_header_id', '=', record.x_studio_consignment_no.id), ('x_studio_purchase_id', '=', record.x_studio_purchase_id.id), ('x_studio_product_id', '=', po_line.product_id.id), ('x_studio_invoice_qty', '=', po_line.quantity), ('x_studio_purchase_line_id', '=', po_line.purchase_line_id.id)],limit=1)
    if con_line_2:
      select += 1
      total_amount += po_line.quantity * (po_line.price_subtotal / po_line.quantity)
      po_line.write({'quantity':con_line_2.x_studio_invoice_qty})
          
      if con_line_2.x_studio_valid_charge == True:
        total_charge += con_line_2.x_studio_tot_charge
        #tot_charge_ids = con_line_2.x_studio_tot_charge_ids
        if con_line_2.x_studio_tot_charge_ids != []:
          for ids in con_line_2.x_studio_tot_charge_ids:
            tot_charge_ids.append(ids.id)
            tot_final_ids.append(ids.id)
      if con_line_2.x_studio_valid_duty == True:
        total_duty += con_line_2.x_studio_tot_duty
        #tot_duty_ids = con_line_2.x_studio_tot_duty_ids
        if con_line_2.x_studio_tot_duty_ids != []:
          for ids in con_line_2.x_studio_tot_duty_ids:
            tot_duty_ids.append(ids.id)
            tot_final_ids.append(ids.id)
      if con_line_2.x_studio_valid_tax == True:
        total_tax += con_line_2.x_studio_tot_tax
        #tot_tax_ids = con_line_2.x_studio_tot_tax_ids
        if con_line_2.x_studio_tot_tax_ids != []:
          for ids in con_line_2.x_studio_tot_tax_ids:
            tot_tax_ids.append(ids.id)
            tot_final_ids.append(ids.id)
        
      if con_line_2.x_studio_valid_charge_cd == True:
        total_charge_cd += con_line_2.x_studio_tot_charge_cd
        #tot_charge_cd_ids = con_line_2.x_studio_tot_charge_cd_ids
        if con_line_2.x_studio_tot_charge_cd_ids != []:
          for ids in con_line_2.x_studio_tot_charge_cd_ids:
            tot_charge_cd_ids.append(ids.id)
            tot_final_cd_ids.append(ids.id)
      if con_line_2.x_studio_valid_duty_cd == True:
        total_duty_cd += con_line_2.x_studio_tot_duty_cd
        #tot_duty_cd_ids = con_line_2.x_studio_tot_duty_cd_ids
        if con_line_2.x_studio_tot_duty_cd_ids != []:
          for ids in con_line_2.x_studio_tot_duty_cd_ids:
            tot_duty_cd_ids.append(ids.id)
            tot_final_cd_ids.append(ids.id)
      if con_line_2.x_studio_valid_tax_cd == True:
        total_tax_cd += con_line_2.x_studio_tot_tax_cd
        #tot_tax_cd_ids = con_line_2.x_studio_tot_tax_cd_ids
        if con_line_2.x_studio_tot_tax_cd_ids != []:
          for ids in con_line_2.x_studio_tot_tax_cd_ids:
            tot_tax_cd_ids.append(ids.id)
            tot_final_cd_ids.append(ids.id)
        
      if con_line_2.x_studio_valid_charge_ex == True:
        total_charge_ex += con_line_2.x_studio_tot_charge_ex
        #tot_charge_ex_ids = con_line_2.x_studio_tot_charge_ex_ids
        if con_line_2.x_studio_tot_charge_ex_ids != []:
          for ids in con_line_2.x_studio_tot_charge_ex_ids:
            tot_charge_ex_ids.append(ids.id)
            tot_final_ex_ids.append(ids.id)
      if con_line_2.x_studio_valid_duty_ex == True:
        total_duty_ex += con_line_2.x_studio_tot_duty_ex
        #tot_duty_ex_ids = con_line_2.x_studio_tot_duty_ex_ids
        if con_line_2.x_studio_tot_duty_ex_ids != []:
          for ids in con_line_2.x_studio_tot_duty_ex_ids:
            tot_duty_ex_ids.append(ids.id)
            tot_final_ex_ids.append(ids.id)
      if con_line_2.x_studio_valid_tax_ex == True:
        total_tax_ex += con_line_2.x_studio_tot_tax_ex
        #tot_tax_ex_ids = con_line_2.x_studio_tot_tax_ex_ids
        if con_line_2.x_studio_tot_tax_ex_ids != []:
          for ids in con_line_2.x_studio_tot_tax_ex_ids:
            tot_tax_ex_ids.append(ids.id)
            tot_final_ex_ids.append(ids.id)
        
      if con_line_2.x_studio_valid_charge_ex_cd == True:
        total_charge_ex_cd += con_line_2.x_studio_tot_charge_ex_cd
        #tot_charge_ex_cd_ids = con_line_2.x_studio_tot_charge_ex_cd_ids
        if con_line_2.x_studio_tot_charge_ex_cd_ids != []:
          for ids in con_line_2.x_studio_tot_charge_ex_cd_ids:
            tot_charge_ex_cd_ids.append(ids.id)
            tot_final_ex_cd_ids.append(ids.id)
      if con_line_2.x_studio_valid_duty_ex_cd == True:
        total_duty_ex_cd += con_line_2.x_studio_tot_duty_ex_cd
        #tot_duty_ex_cd_ids = con_line_2.x_studio_tot_duty_ex_cd_ids
        if con_line_2.x_studio_tot_duty_ex_cd_ids != []:
          for ids in con_line_2.x_studio_tot_duty_ex_cd_ids:
            tot_duty_ex_cd_ids.append(ids.id)
            tot_final_ex_cd_ids.append(ids.id)
      if con_line_2.x_studio_valid_tax_ex_cd == True:
        total_tax_ex_cd += con_line_2.x_studio_tot_tax_ex_cd
        #tot_tax_ex_cd_ids = con_line_2.x_studio_tot_tax_ex_cd_ids
        if con_line_2.x_studio_tot_tax_ex_cd_ids != []:
          for ids in con_line_2.x_studio_tot_tax_ex_cd_ids:
            tot_tax_ex_cd_ids.append(ids.id)
            tot_final_ex_cd_ids.append(ids.id)
  
  for po_line_count in record.invoice_line_ids:
    if po_line_count.quantity > 0.0000:
      done += 1
  
  if done == 0:
    raise UserError('You have not recorded done quantities yet, Atleast one done quantity should be recorded to proceed.') 
  
  if select == 0:
    raise UserError('The Selected Consignment No is not linked with the Correct Items & Item Quantities in the Current PO.')

  con_header = env['x_consignment_header'].search([('id', '=', record.x_studio_consignment_no.id)],limit=1)
  grand_total_amount = 0
  if con_header:
    journal = env['account.journal'].search([('name', '=', 'Vendor Despatch'),('company_id', '=', company.id)], limit=1) 
    if not journal:
     raise UserError('The required journal has not been setup. Process terminated.')
           
    if con_header.x_studio_vendor_dispatch == True:
      grand_total_amount = total_amount * con_header.x_studio_vendor_dispatch_exchange_rate
      if grand_total_amount > 0.0000:
        despatch_account = env['x_imports_ledger_setup'].search([('x_studio_company_id', '=', company.id)], limit=1)
        if despatch_account:
          if despatch_account.x_studio_vendor_despatch_debit_account.id == 0 or despatch_account.x_studio_vendor_despatch_credit_account.id == 0:
            raise UserError('Vendor Despatch Accounts must be Specified in Imports Ledger Setup')
            
        despatch_lines=[]
        despatch_lines.append([0,0,{
          'account_id':despatch_account.x_studio_vendor_despatch_credit_account.id,
          'partner_id':record.partner_id.id,
          'name':'Vendor Despatch Reversal',
          'debit':round(grand_total_amount,2)}])
                  
        despatch_lines.append([0,0,{
          'account_id':despatch_account.x_studio_vendor_despatch_debit_account.id,
          'partner_id':record.partner_id.id,
          'name':'Vendor Despatch Reversal',
          'credit':round(grand_total_amount,2)}])	
        
        vendor_despatch = env['account.move'].create({'x_studio_created_from_vendor_bill':record.id,'journal_id':journal.id,'move_type':'entry','line_ids':despatch_lines})
              
        update_vendor_despatch = env['account.move'].search([('id', '=', vendor_despatch.id)],limit=1)
        if update_vendor_despatch:
          update_vendor_despatch.write({'state':'posted'})
        
    if con_header.x_studio_custom_clearance == True: 
      despatch_lines_2=[]
      if total_charge > 0.0000:
        despatch_lines_2.append([0,0,{
        'account_id':con_header.x_studio_account_charge.id,
        'name':'Purchase, Charge - Reversal',
        'credit':round((total_charge * con_header.x_studio_custom_clearance_exchange_rate),2)}])
          
      if total_duty > 0.0000:  
        despatch_lines_2.append([0,0,{
        'account_id':con_header.x_studio_account_duty.id,
        'name':'Purchase Misc. Charges Duty - Reversal',
        'credit':round((total_duty * con_header.x_studio_custom_clearance_exchange_rate),2)}])
          
      if total_tax > 0.0000:  
        despatch_lines_2.append([0,0,{
        'account_id':con_header.x_studio_account_tax.id,
        'name':'Purchase, Packing Slip Tax - Reversal',
        'credit':round((total_tax * con_header.x_studio_custom_clearance_exchange_rate),2)}])
      
      if (total_charge + total_duty + total_tax) > 0.0000:    
        despatch_lines_2.append([0,0,{
        'account_id':con_header.x_studio_offset_account.id,
        'name':'Purchase Packing Slip Offset - Reversal',
        'debit':(round((total_charge * con_header.x_studio_custom_clearance_exchange_rate),2) + round((total_duty * con_header.x_studio_custom_clearance_exchange_rate),2) + round((total_tax * con_header.x_studio_custom_clearance_exchange_rate),2))}])	
      
      custom_clearance = env['account.move'].create({'x_studio_created_from_vendor_bill_1':record.id,'journal_id':journal.id,'move_type':'entry','line_ids':despatch_lines_2})
          
      update_custom_clearance = env['account.move'].search([('id', '=', custom_clearance.id)],limit=1)
      if update_custom_clearance:
        update_custom_clearance.write({'state':'posted'})
        
###############################################################################################################################################################        
  invoice_lines=[]
  
  if total_charge > 0.0000:
    product_charge = env['product.product'].search([('x_studio_charge_type', '=', 'Charge'),('type', '=', 'service'),('x_studio_non_billable', '=', False)], limit=1)
    if product_charge:
      val_charge = True
      
      invoice_lines.append([0,0,{
        'move_id':record.id,
        'partner_id':record.partner_id.id,
        'product_id':product_charge.id,
        'quantity':1,
        'currency_id':record.currency_id.id,
        'name':product_charge.name,
        'product_uom_id':product_charge.uom_id.id,
        'is_landed_costs_line':True,
        'price_unit':round(total_charge/ex_rate,2),
        'price_subtotal':round(total_charge/ex_rate,2),
        'amount_currency':round(total_charge/ex_rate,2),
        'debit':round(total_charge/ex_rate,2),
        'x_studio_tags':[(6, 0, tot_charge_ids)],
        'account_id':product_charge.categ_id.property_stock_account_input_categ_id.id}])
      
  if total_duty > 0.0000:    
    product_duty = env['product.product'].search([('x_studio_charge_type', '=', 'Duty'),('type', '=', 'service'),('x_studio_non_billable', '=', False)], limit=1)
    if product_duty:
      val_duty = True
      
      invoice_lines.append([0,0,{
        'move_id':record.id,
        'partner_id':record.partner_id.id,
        'product_id':product_duty.id,
        'quantity':1,
        'currency_id':record.currency_id.id,
        'name':product_duty.name,
        'product_uom_id':product_duty.uom_id.id,
        'is_landed_costs_line':True,
        'price_unit':round(total_duty/ex_rate,2),
        'price_subtotal':round(total_duty/ex_rate,2),
        'amount_currency':round(total_duty/ex_rate,2),
        'debit':round(total_duty/ex_rate,2),
        'x_studio_tags':[(6, 0, tot_duty_ids)],
        'account_id':product_duty.categ_id.property_stock_account_input_categ_id.id}])
      
  if total_tax > 0.0000:
    product_tax = env['product.product'].search([('x_studio_charge_type', '=', 'Tax'),('type', '=', 'service'),('x_studio_non_billable', '=', False)], limit=1)
    if product_tax:
      val_tax = True
      
      invoice_lines.append([0,0,{
        'move_id':record.id,
        'partner_id':record.partner_id.id,
        'product_id':product_tax.id,
        'quantity':1,
        'currency_id':record.currency_id.id,
        'name':product_tax.name,
        'product_uom_id':product_tax.uom_id.id,
        'is_landed_costs_line':True,
        'price_unit':round(total_tax/ex_rate,2),
        'price_subtotal':round(total_tax/ex_rate,2),
        'amount_currency':round(total_tax/ex_rate,2),
        'debit':round(total_tax/ex_rate,2),
        'x_studio_tags':[(6, 0, tot_tax_ids)],
        'account_id':product_tax.categ_id.property_stock_account_input_categ_id.id}])
  ##########################
  creditor_acc = env['account.move.line'].search([('move_id', '=', record.id), ('account_id', '=', record.partner_id.property_account_payable_id.id)],limit=1)
  ##########################
  #raise UserError(str(round(total_charge/ex_rate,2)) + "\n" + str(round(total_duty/ex_rate,2)) + "\n" + str(round(total_tax/ex_rate,2)) + "\n" + str((round(total_charge/ex_rate,2) + round(total_duty/ex_rate,2) + round(total_tax/ex_rate,2))))   
  if val_charge == True or val_duty == True or val_tax == True:
    tp_account = env['x_imports_ledger_setup'].search([('id', '=', 1)], limit=1)
    if tp_account:
      if tp_account.x_studio_import_tp_vendor_account.id == 0:
        raise UserError('Import TP Vendor Account must be Specified in Imports Ledger Setup')
      else:
        invoice_lines.append([0,0,{
          'move_id':record.id,
          'partner_id':record.partner_id.id,
          'quantity':1,
          'date_maturity':creditor_acc.date_maturity,
          'currency_id':record.currency_id.id,
          'name':'Total - Billable Invoice Lines', #'name':tp_account.x_studio_import_tp_vendor_account.name,
          'price_unit':-(round(total_charge/ex_rate,2) + round(total_duty/ex_rate,2) + round(total_tax/ex_rate,2)),
          'price_subtotal':-(round(total_charge/ex_rate,2) + round(total_duty/ex_rate,2) + round(total_tax/ex_rate,2)),
          'amount_currency':-(round(total_charge/ex_rate,2) + round(total_duty/ex_rate,2) + round(total_tax/ex_rate,2)),
          'credit':(round(total_charge/ex_rate,2) + round(total_duty/ex_rate,2) + round(total_tax/ex_rate,2)),
          'account_id':tp_account.x_studio_import_tp_vendor_account.id,
          'x_studio_tags':[(6, 0, tot_final_ids)],
          'exclude_from_invoice_tab':True}])
      
        record.with_context(check_move_validity = True).write({'line_ids':invoice_lines})
        
######################################################################-------Mod-1-------###################################################################################
  invoice_lines_ex=[]
  
  if total_charge_ex > 0.0000:
    product_charge_ex = env['product.product'].search([('x_studio_charge_type', '=', 'Charge'),('type', '=', 'service'),('x_studio_non_billable', '=', True)], limit=1)
    if product_charge_ex:
      val_charge_ex = True
      
      invoice_lines_ex.append([0,0,{
        'move_id':record.id,
        'partner_id':record.partner_id.id,
        'product_id':product_charge_ex.id,
        'quantity':1,
        'currency_id':record.currency_id.id,
        'name':product_charge_ex.name,
        'product_uom_id':product_charge_ex.uom_id.id,
        'is_landed_costs_line':True,
        'price_unit':round(total_charge_ex/ex_rate,2),
        'price_subtotal':round(total_charge_ex/ex_rate,2),
        'amount_currency':round(total_charge_ex/ex_rate,2),
        'debit':round(total_charge_ex/ex_rate,2),
        'x_studio_tags':[(6, 0, tot_charge_ex_ids)],
        'account_id':product_charge_ex.categ_id.property_stock_account_input_categ_id.id}])
      
  if total_duty_ex > 0.0000:    
    product_duty_ex = env['product.product'].search([('x_studio_charge_type', '=', 'Duty'),('type', '=', 'service'),('x_studio_non_billable', '=', True)], limit=1)
    if product_duty_ex:
      val_duty_ex = True
      
      invoice_lines_ex.append([0,0,{
        'move_id':record.id,
        'partner_id':record.partner_id.id,
        'product_id':product_duty_ex.id,
        'quantity':1,
        'currency_id':record.currency_id.id,
        'name':product_duty_ex.name,
        'product_uom_id':product_duty_ex.uom_id.id,
        'is_landed_costs_line':True,
        'price_unit':round(total_duty_ex/ex_rate,2),
        'price_subtotal':round(total_duty_ex/ex_rate,2),
        'amount_currency':round(total_duty_ex/ex_rate,2),
        'debit':round(total_duty_ex/ex_rate,2),
        'x_studio_tags':[(6, 0, tot_duty_ex_ids)],
        'account_id':product_duty_ex.categ_id.property_stock_account_input_categ_id.id}])
      
  if total_tax_ex > 0.0000:
    product_tax_ex = env['product.product'].search([('x_studio_charge_type', '=', 'Tax'),('type', '=', 'service'),('x_studio_non_billable', '=', True)], limit=1)
    if product_tax_ex:
      val_tax_ex = True
      
      invoice_lines_ex.append([0,0,{
        'move_id':record.id,
        'partner_id':record.partner_id.id,
        'product_id':product_tax_ex.id,
        'quantity':1,
        'currency_id':record.currency_id.id,
        'name':product_tax_ex.name,
        'product_uom_id':product_tax_ex.uom_id.id,
        'is_landed_costs_line':True,
        'price_unit':round(total_tax_ex/ex_rate,2),
        'price_subtotal':round(total_tax_ex/ex_rate,2),
        'amount_currency':round(total_tax_ex/ex_rate,2),
        'debit':round(total_tax_ex/ex_rate,2),
        'x_studio_tags':[(6, 0, tot_tax_ex_ids)],
        'account_id':product_tax_ex.categ_id.property_stock_account_input_categ_id.id}])
  #raise UserError(str(round(total_charge_ex/ex_rate,2)) + "\n" + str(round(total_duty_ex/ex_rate,2)) + "\n" + str(round(total_tax_ex/ex_rate,2)) + "\n" + str((round(total_charge_ex/ex_rate,2) + round(total_duty_ex/ex_rate,2) + round(total_tax_ex/ex_rate,2))) + "\n" + str((round(total_charge_ex,2) + round(total_duty_ex,2) + round(total_tax_ex,2))))  
  if val_charge_ex == True or val_duty_ex == True or val_tax_ex == True:
    tp_account_ex = env['x_imports_ledger_setup'].search([('id', '=', 1)], limit=1)
    if tp_account_ex:
      if tp_account_ex.x_studio_import_tp_vend_acc_non_billable.id == 0:
        raise UserError('Import TP Non Billable Vendor Account must be Specified in Imports Ledger Setup')
      else:
        invoice_lines_ex.append([0,0,{
          'move_id':record.id,
          'partner_id':record.partner_id.id,
          'quantity':1,
          'currency_id':record.currency_id.id,
          'name':'Total - Non Billable Invoice Lines', #'name':tp_account_ex.x_studio_import_tp_vend_acc_non_billable.name,
          'price_unit':-(round(total_charge_ex/ex_rate,2) + round(total_duty_ex/ex_rate,2) + round(total_tax_ex/ex_rate,2)),
          'price_subtotal':-(round(total_charge_ex/ex_rate,2) + round(total_duty_ex/ex_rate,2) + round(total_tax_ex/ex_rate,2)),
          'amount_currency':-(round(total_charge_ex/ex_rate,2) + round(total_duty_ex/ex_rate,2) + round(total_tax_ex/ex_rate,2)),
          'credit':(round(total_charge_ex/ex_rate,2) + round(total_duty_ex/ex_rate,2) + round(total_tax_ex/ex_rate,2)),
          'account_id':tp_account_ex.x_studio_import_tp_vend_acc_non_billable.id,
          'x_studio_tags':[(6, 0, tot_final_ex_ids)],
          'exclude_from_invoice_tab':True}])
      
        record.with_context(check_move_validity = True).write({'line_ids':invoice_lines_ex})
######################################################################-------Mod-1-------###################################################################################

######################################################################-------Mod-2-------###################################################################################
  invoice_lines_cd=[]
  
  tp_account_debit = env['x_imports_ledger_setup'].search([('id', '=', 1)], limit=1)
  if tp_account_debit:
    if tp_account_debit.x_studio_import_tp_vendor_debit_acc.id == 0:
      raise UserError('Import TP Vendor Debit Account must be Specified in Imports Ledger Setup')
    else:
      if total_charge_cd > 0.0000:
        val_charge_cd = True
            
        invoice_lines_cd.append([0,0,{
          'move_id':record.id,
          'account_id':tp_account_debit.x_studio_import_tp_vendor_debit_acc.id,
          'partner_id':record.partner_id.id,
          'name':'Charge - TP Invoice Line', #'name':'Charge - TP Debit Account',
          'currency_id':record.currency_id.id,
          'amount_currency':round(total_charge_cd/ex_rate,2),
          'x_studio_tags':[(6, 0, tot_charge_cd_ids)],
          'debit':round(total_charge_cd/ex_rate,2)}])
            
      if total_duty_cd > 0.0000:    
        val_duty_cd = True
            
        invoice_lines_cd.append([0,0,{
          'move_id':record.id,
          'account_id':tp_account_debit.x_studio_import_tp_vendor_debit_acc.id,
          'partner_id':record.partner_id.id,
          'name':'Duty - TP Invoice Line', #'name':'Duty - TP Debit Account',
          'currency_id':record.currency_id.id,
          'amount_currency':round(total_duty_cd/ex_rate,2),
          'x_studio_tags':[(6, 0, tot_duty_cd_ids)],
          'debit':round(total_duty_cd/ex_rate,2)}])
            
      if total_tax_cd > 0.0000:
        val_tax_cd = True
            
        invoice_lines_cd.append([0,0,{
          'move_id':record.id,
          'account_id':tp_account_debit.x_studio_import_tp_vendor_debit_acc.id,
          'partner_id':record.partner_id.id,
          'name':'Tax - TP Invoice Line', #'name':'Tax - TP Debit Account',
          'currency_id':record.currency_id.id,
          'amount_currency':round(total_tax_cd/ex_rate,2),
          'x_studio_tags':[(6, 0, tot_tax_cd_ids)],
          'debit':round(total_tax_cd/ex_rate,2)}])
      ##########################
      creditor_acc = env['account.move.line'].search([('move_id', '=', record.id), ('account_id', '=', record.partner_id.property_account_payable_id.id)],limit=1)
      ##########################
      #raise UserError(str(round(total_charge_cd/ex_rate,2)) + "\n" + str(round(total_duty_cd/ex_rate,2)) + "\n" + str(round(total_tax_cd/ex_rate,2)) + "\n" + str((round(total_charge_cd/ex_rate,2) + round(total_duty_cd/ex_rate,2) + round(total_tax_cd/ex_rate,2))))    
      if val_charge_cd == True or val_duty_cd == True or val_tax_cd == True:
        tp_account_cd = env['x_imports_ledger_setup'].search([('id', '=', 1)], limit=1)
        if tp_account_cd:
          if tp_account_cd.x_studio_import_tp_vendor_account.id == 0:
            raise UserError('Import TP Vendor Account must be Specified in Imports Ledger Setup')
          else:
            invoice_lines_cd.append([0,0,{
              'move_id':record.id,
              'partner_id':record.partner_id.id,
              'date_maturity':creditor_acc.date_maturity,
              'currency_id':record.currency_id.id,
              'name':'Total - TP Invoice Lines', #'name':tp_account_cd.x_studio_import_tp_vendor_account.name,
              'amount_currency':-(round(total_charge_cd/ex_rate,2) + round(total_duty_cd/ex_rate,2) + round(total_tax_cd/ex_rate,2)),
              'credit':(round(total_charge_cd/ex_rate,2) + round(total_duty_cd/ex_rate,2) + round(total_tax_cd/ex_rate,2)),
              'account_id':tp_account_cd.x_studio_import_tp_vendor_account.id,
              'x_studio_tags':[(6, 0, tot_final_cd_ids)],
              'exclude_from_invoice_tab':True}])
            
            record.with_context(check_move_validity = True).write({'line_ids':invoice_lines_cd})
######################################################################-------Mod-2-------###################################################################################

######################################################################-------Mod-3-------###################################################################################
  invoice_lines_ex_cd=[]
  
  tp_account_non_billable_debit = env['x_imports_ledger_setup'].search([('id', '=', 1)], limit=1)
  if tp_account_non_billable_debit:
    if tp_account_non_billable_debit.x_studio_import_tp_vend_non_billable_debit_acc.id == 0:
      raise UserError('Import TP Vendor Non Billable Debit Account must be Specified in Imports Ledger Setup')
    else:
      
      if total_charge_ex_cd > 0.0000:
        val_charge_ex_cd = True
            
        invoice_lines_ex_cd.append([0,0,{
          'move_id':record.id,
          'account_id':tp_account_non_billable_debit.x_studio_import_tp_vend_non_billable_debit_acc.id,
          'partner_id':record.partner_id.id,
          'name':'TP Charges - Non-Billable - Load to GL', #'name':'Charge - TP Non Billable Invoice Lines', #'name':'Charge - TP Non Billable Debit Account',
          'currency_id':record.currency_id.id,
          'amount_currency':round(total_charge_ex_cd/ex_rate,2),
          'x_studio_tags':[(6, 0, tot_charge_ex_cd_ids)],
          'debit':round(total_charge_ex_cd/ex_rate,2)}])
            
      if total_duty_ex_cd > 0.0000:    
        val_duty_ex_cd = True
            
        invoice_lines_ex_cd.append([0,0,{
          'move_id':record.id,
          'account_id':tp_account_non_billable_debit.x_studio_import_tp_vend_non_billable_debit_acc.id,
          'partner_id':record.partner_id.id,
          'name':'Duty - TP Non Billable Invoice Lines', #'name':'Duty - TP Non Billable Debit Account',
          'currency_id':record.currency_id.id,
          'amount_currency':round(total_duty_ex_cd/ex_rate,2),
          'x_studio_tags':[(6, 0, tot_duty_ex_cd_ids)],
          'debit':round(total_duty_ex_cd/ex_rate,2)}])
            
      if total_tax_ex_cd > 0.0000:
        val_tax_ex_cd = True
            
        invoice_lines_ex_cd.append([0,0,{
          'move_id':record.id,
          'account_id':tp_account_non_billable_debit.x_studio_import_tp_vend_non_billable_debit_acc.id,
          'partner_id':record.partner_id.id,
          'name':'TP Tax - Non-Billable - Load to GL', #'name':'Tax - TP Non Billable Invoice Lines', #'name':'Tax - TP Non Billable Debit Account',
          'currency_id':record.currency_id.id,
          'amount_currency':round(total_tax_ex_cd/ex_rate,2),
          'x_studio_tags':[(6, 0, tot_tax_ex_cd_ids)],
          'debit':round(total_tax_ex_cd/ex_rate,2)}])
      
      if val_charge_ex_cd == True or val_duty_ex_cd == True or val_tax_ex_cd == True:
        tp_account_ex_cd = env['x_imports_ledger_setup'].search([('id', '=', 1)], limit=1)
        if tp_account_ex_cd:
          if tp_account_ex_cd.x_studio_import_tp_vend_acc_non_billable.id == 0:
            raise UserError('Import TP Non Billable Vendor Account must be Specified in Imports Ledger Setup')
          else:
            #tot_final_ex_cd_ids = (tot_charge_ex_cd_ids + tot_duty_ex_cd_ids + tot_tax_ex_cd_ids)######
            invoice_lines_ex_cd.append([0,0,{
              'move_id':record.id,
              'partner_id':record.partner_id.id,
              'currency_id':record.currency_id.id,
              'name':'Total - TP Non Billable Invoice Lines', #'name':tp_account_ex_cd.x_studio_import_tp_vend_acc_non_billable.name,
              'amount_currency':-(round(total_charge_ex_cd/ex_rate,2) + round(total_duty_ex_cd/ex_rate,2) + round(total_tax_ex_cd/ex_rate,2)),
              'credit':(round(total_charge_ex_cd/ex_rate,2) + round(total_duty_ex_cd/ex_rate,2) + round(total_tax_ex_cd/ex_rate,2)),
              'account_id':tp_account_ex_cd.x_studio_import_tp_vend_acc_non_billable.id,
              'x_studio_tags':[(6, 0, tot_final_ex_cd_ids)],
              'exclude_from_invoice_tab':True}])
        
            record.with_context(check_move_validity = True).write({'line_ids':invoice_lines_ex_cd})
######################################################################-------Mod-3-------###################################################################################

#if (charge + duty + tax) > 0.0000:
#    record.with_context(check_move_validity = False).write({'line_ids':invoice_lines}) 

#    create_charges = env['account.move.line'].with_context(check_move_validity = False).create({'move_id':record.id,'partner_id':record.partner_id.id,'product_id':product_charge.id,'quantity':1,'currency_id':record.currency_id.id,'name':product_charge.name,'product_uom_id':product_charge.uom_id.id,'is_landed_costs_line':True,'price_unit':10.00,'price_subtotal':10.00,'account_id':4})
#    payable_account = env['account.move.line'].with_context(check_move_validity = True).search([('move_id', '=', record.id), ('account_id', '=', record.partner_id.property_account_payable_id.id)], limit=1)
#    if payable_account:
#      payable_account.write({'credit':(payable_account.credit + (charge + duty + tax))})
   
  record['x_studio_update_consignment'] = True]]></field>
    </record>

    <!-- 2366: IMP - Vendor Bill Currency Rate. FULL PORT (v0.0.43).
         Rewrites local-currency debit/credit on every ledger line of
         the invoice using record.x_studio_currency_rate as the new
         rate against each line's amount_currency. Uses
         with_context(check_move_validity=False) so lines can be
         updated individually without tripping Odoo's balance-check
         mid-way. Then flags x_studio_currency_rate_updated=True.

         Byte-verbatim port including Clear-DB's commented-out
         docstring at the bottom (keeps parity - the docstring is
         a memo they left for themselves). -->
    <record id="srv_imp_vendor_bill_currency_rate" model="ir.actions.server">
        <field name="name">IMP - Vendor Bill Currency Rate</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.x_studio_currency_rate > 0.00:
    tot_amount = 0

    #record.with_context(check_move_validity = False)

    for invoice_lines2 in record.line_ids:
        if invoice_lines2.credit == 0.00:
            invoice_lines2.with_context(check_move_validity=False).write({'debit': (record.x_studio_currency_rate * abs(invoice_lines2.amount_currency))})
        if invoice_lines2.debit == 0.00:
            invoice_lines2.with_context(check_move_validity=False).write({'credit': (record.x_studio_currency_rate * abs(invoice_lines2.amount_currency))})

    record['x_studio_currency_rate_updated'] = True
else:
    raise UserError('Currency Rate must be defined!')

#record.with_context(check_move_validity = True).write({'line_ids':invoice_lines_cd})

"""  credit_line = self.env['account.move.line'].with_context(
            check_move_validity=False).create({
            'move_id': self.journal_entry.id,
            'account_id': self.product.revenue_account,
            'partner_id': self.container.partner.id,
            'name': 'Finish '+self.job_name,
            'credit': self.cost
         })"""
]]></field>
    </record>

    <!-- ==================================================== -->
    <!-- 1493/1495 SLS Credit Note Approval multi-state pair  -->
    <!-- FULL PORT (v0.0.44). Two multi actions orchestrating -->
    <!-- 4 child actions:                                     -->
    <!--                                                      -->
    <!--   1493 Request Approval [children in order: 1491, 1489]  -->
    <!--     1491 -> next_activity assigned to Tharaka H      -->
    <!--     1489 -> object_write x_studio_credit_note_request_sent=True -->
    <!--                                                      -->
    <!--   1495 Approval Main [children in order: 2537, 2539] -->
    <!--     2537 -> object_write x_studio_credit_note_approved=True -->
    <!--     2539 -> next_activity generic (record.user_id)   -->
    <!--                                                      -->
    <!-- Tharaka reference uses seed_master_data_and_settings -->
    <!-- .user_146 (stable xmlid on repair-test-101).         -->
    <!-- ==================================================== -->

    <record id="srv_sls_credit_note_notify_tharaka" model="ir.actions.server">
        <field name="name">SLS - Request Credit Note Approval - Notify User</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="state">next_activity</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="activity_type_id" ref="mail.mail_activity_data_todo"/>
        <field name="activity_summary">Approve Credit Note</field>
        <field name="activity_note"><![CDATA[<p><br></p>]]></field>
        <field name="activity_date_deadline_range">0</field>
        <field name="activity_date_deadline_range_type">days</field>
        <field name="activity_user_type">specific</field>
        <field name="activity_user_id" ref="seed_master_data_and_settings.user_146"/>
        <field name="activity_user_field_name">user_id</field>
    </record>

    <record id="srv_sls_credit_note_request_sent_flag" model="ir.actions.server">
        <field name="name">SLS - Request Credit Note Approval Sent</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="state">object_write</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="update_path">x_studio_credit_note_request_sent</field>
        <field name="update_field_id" ref="field_account_move__x_studio_credit_note_request_sent"/>
        <field name="update_boolean_value">true</field>
        <field name="value">Yes</field>
    </record>

    <record id="srv_sls_credit_note_approved_flag" model="ir.actions.server">
        <field name="name">SLS - Credit Note Approval</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="state">object_write</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="update_path">x_studio_credit_note_approved</field>
        <field name="update_field_id" ref="field_account_move__x_studio_credit_note_approved"/>
        <field name="update_boolean_value">true</field>
        <field name="value">Yes</field>
    </record>

    <record id="srv_sls_credit_note_confirm_activity" model="ir.actions.server">
        <field name="name">SLS - Credit Note Approval - Confirm</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="state">next_activity</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="activity_type_id" ref="mail.mail_activity_data_todo"/>
        <field name="activity_summary">Credit Note Approval - Confirm</field>
        <field name="activity_note"><![CDATA[<p><br></p>]]></field>
        <field name="activity_date_deadline_range">0</field>
        <field name="activity_date_deadline_range_type">days</field>
        <field name="activity_user_type">generic</field>
        <field name="activity_user_field_name">user_id</field>
    </record>

    <record id="srv_sls_request_credit_note_approval" model="ir.actions.server">
        <field name="name">SLS - Request Credit Note Approval</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">multi</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="child_ids" eval="[(6, 0, [ref('srv_sls_credit_note_notify_tharaka'), ref('srv_sls_credit_note_request_sent_flag')])]"/>
    </record>

    <record id="srv_sls_credit_note_approval_main" model="ir.actions.server">
        <field name="name">SLS - Credit Note Approval - Main</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">multi</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="child_ids" eval="[(6, 0, [ref('srv_sls_credit_note_approved_flag'), ref('srv_sls_credit_note_confirm_activity')])]"/>
    </record>

    <!-- 1854: SLS - View Credit Limit Validation in Customer Invoice.
         FULL PORT (v0.0.38). Gates customer invoice posting: raises
         UserError if the customer's outstanding receivable + this
         invoice's amount exceeds their configured credit limit,
         BUT only when order payment method is Credit. Fields:
         partner_id (standard), amount_total (standard),
         x_studio_order_payment_method (Studio Selection on
         account.move, already declared in models/account_move.py). -->
    <record id="srv_sls_view_credit_limit_validation" model="ir.actions.server">
        <field name="name">SLS - View Credit Limit Validation in Customer Invoice</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.partner_id.customer_rank > 0:
    if record.partner_id.id:
        if record.x_studio_order_payment_method == 'Credit':
            if (record.partner_id.credit + record.amount_total) > record.partner_id.credit_limit:
                raise UserError(
                    "Customer's credit limit is exceeded." + "\n"
                    + "Customer credit limit: " + str(record.partner_id.credit_limit) + "\n"
                    + "Cust. Total Receivable: " + str(record.partner_id.credit) + "\n"
                    + "Current Tot. Amount: " + str(record.amount_total) + "\n"
                    + "Over Credit Amount: " + str((record.partner_id.credit + record.amount_total) - record.partner_id.credit_limit)
                )
]]></field>
    </record>

    <!-- 1851: SLS - Send Bank Guarantee Notification in Customer Invoice.
         FULL PORT (v0.0.41). Non-blocking gate on customer invoices:
         posts a chatter warning (does NOT raise) when the customer's
         bank guarantee is expired or insufficient for the credit + this
         invoice. Only fires for non-'General' customer groups where BG
         is optional (x_studio_mandatory_bank_guarantee == False).
         Marks x_studio_bg_sent=True after posting to avoid duplicates. -->
    <record id="srv_sls_send_bank_guarantee_notification" model="ir.actions.server">
        <field name="name">SLS - Send Bank Guarantee Notification in Customer Invoice</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.partner_id.customer_rank > 0:
    if record.move_type == 'out_invoice':
        if record.partner_id.x_studio_customer_group.x_studio_group_type != 'General':
            if record.partner_id.x_studio_mandatory_bank_guarantee == False:
                if record.partner_id.x_studio_expiry_date and record.partner_id.x_studio_expiry_date < datetime.date.today():
                    records.message_post(body="Customer's bank guarantee is expired." + "\n" + "Bank guaranty expiry date: " + str(record.partner_id.x_studio_expiry_date))
                    record['x_studio_bg_sent'] = True
                else:
                    if record.partner_id.x_studio_bank_guarantee_amount < (record.partner_id.credit + record.amount_total):
                        records.message_post(body="The credit limit is higher than the bank guarantee amount." + "\n" + "Customer: " + str(record.partner_id.name) + "\n" + " Sales Order: " + str(record.x_studio_sale_id.name) + "\n" + "Bank guarantee amount: " + str(record.partner_id.x_studio_bank_guarantee_amount) + "\n" + "Current Balance: " + str(record.partner_id.credit) + '\n' + "This Sale: " + str(record.amount_total) + "\n" + "New balance: " + str(record.partner_id.credit + record.amount_total))
                        record['x_studio_bg_sent'] = True
]]></field>
    </record>

    <!-- 1852: SLS - View Bank Guarantee Validation in Customer Invoice.
         FULL PORT (v0.0.42). Blocking counterpart to 1851: raises
         UserError (not chatter post) when BG is expired or insufficient
         AND the customer's group requires mandatory BG
         (x_studio_mandatory_bank_guarantee == True). Same field
         dependencies as 1851; no new deps. -->
    <record id="srv_sls_view_bank_guarantee_validation" model="ir.actions.server">
        <field name="name">SLS - View Bank Guarantee Validation in Customer Invoice</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.partner_id.customer_rank > 0:
    if record.move_type == 'out_invoice':
        if record.partner_id.x_studio_customer_group.x_studio_group_type != 'General':
            if record.partner_id.x_studio_mandatory_bank_guarantee == True:
                if record.partner_id.x_studio_expiry_date and record.partner_id.x_studio_expiry_date < datetime.date.today():
                    raise UserError("Customer's bank guarantee is expired." + "\n" + "Bank guaranty expiry date: " + str(record.partner_id.x_studio_expiry_date))
                else:
                    if record.partner_id.x_studio_bank_guarantee_amount < (record.partner_id.credit + record.amount_total):
                        raise UserError("Insufficient bank guarantee." + "\n" + "Customer: " + record.partner_id.name + "\n" + "Sales Order: " + record.x_studio_sale_id.name + "\n" + "Bank guarantee amount: " + str(record.partner_id.x_studio_bank_guarantee_amount) + "\n" + "Current Balance: " + str(record.partner_id.credit) + '\n' + "This Sale: " + str(record.amount_total) + "\n" + "New balance: " + str(record.partner_id.credit + record.amount_total))
]]></field>
    </record>

    <!-- ==================================================== -->
    <!-- Window actions (2) — ported verbatim from Clear-DB   -->
    <!-- ==================================================== -->

    <record id="act_dispatch_reversal" model="ir.actions.act_window">
        <field name="name">Dispatch Reversal</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('x_studio_created_from_vendor_bill', '=', active_id)]</field>
        <field name="context">{'search_default_x_studio_created_from_vendor_bill': active_id, 'default_x_studio_created_from_vendor_bill': active_id}</field>
    </record>

    <record id="act_custom_clearance_reversal" model="ir.actions.act_window">
        <field name="name">Custom Clearance Reversal</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('x_studio_created_from_vendor_bill_1', '=', active_id)]</field>
        <field name="context">{'search_default_x_studio_created_from_vendor_bill_1': active_id, 'default_x_studio_created_from_vendor_bill_1': active_id}</field>
    </record>

    <!-- ==================================================== -->
    <!-- v0.0.24 additions: 4 more numeric action refs        -->
    <!-- caught by a broader sweep than v0.0.20 did.          -->
    <!--                                                      -->
    <!-- Clear-DB id -> local xmlid:                          -->
    <!--   2739 -> srv_advance_payment_acc_update (FULL PORT) -->
    <!--   1310 -> act_custom_currency_rates                  -->
    <!--   1676 -> srv_sales_report_sample_button (verbatim)  -->
    <!--   1386 -> act_tp_invoice_vendor_bill                 -->
    <!-- ==================================================== -->

    <!-- 2739: Project - Update Advance Payment Account.
         Full port of the Clear-DB Python — this is a real business
         rule, not a stub. Referenced by
         views/account_payment_studio_ported_v2.xml line 26. Depends
         on x_advance_payment_acco model (already ported in v0.0.14). -->
    <record id="srv_advance_payment_acc_update" model="ir.actions.server">
        <field name="name">Project - Update Advance Payment Account</field>
        <field name="model_id" ref="account.model_account_payment"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">base_automation</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.x_studio_type == 'Advance Payment':
    account = env['x_advance_payment_acco'].search([('id', '=', 1)], limit=1)
    if account:
        if record.partner_type == 'customer':
            if account.x_studio_advance_payment_account_sales == False:
                raise UserError('Advance Payment Account (Sales) must be Specified in Accounting Configuration')

            journal_entry = env['account.move'].search([('payment_id', '=', record.id)], limit=1)
            if journal_entry:
                if record.payment_type == 'inbound':
                    lines = env['account.move.line'].search([('move_id', '=', journal_entry.id), ('debit', '>', 0)], limit=1)
                    if lines:
                        lines.write({'account_id': account.x_studio_advance_payment_account_sales.id})
                        record.write({'x_studio_advance_payment_acc_updated': True})
                    else:
                        raise UserError('There is no payment line to update.')
                else:
                    lines = env['account.move.line'].search([('move_id', '=', journal_entry.id), ('credit', '>', 0)], limit=1)
                    if lines:
                        lines.write({'account_id': account.x_studio_advance_payment_account_sales.id})
                        record.write({'x_studio_advance_payment_acc_updated': True})
                    else:
                        raise UserError('There is no payment line to update.')
            else:
                raise UserError('There is no payment to update.')
        else:
            if account.x_studio_advance_payment_account_purchases_1 == False:
                raise UserError('Advance Payment Account (Purchases) must be Specified in Accounting Configuration')

            journal_entry = env['account.move'].search([('payment_id', '=', record.id)], limit=1)
            if journal_entry:
                if record.payment_type == 'inbound':
                    lines = env['account.move.line'].search([('move_id', '=', journal_entry.id), ('debit', '>', 0)], limit=1)
                    if lines:
                        lines.write({'account_id': account.x_studio_advance_payment_account_purchases_1.id})
                        record.write({'x_studio_advance_payment_acc_updated': True})
                    else:
                        raise UserError('There is no payment line to update.')
                else:
                    lines = env['account.move.line'].search([('move_id', '=', journal_entry.id), ('credit', '>', 0)], limit=1)
                    if lines:
                        lines.write({'account_id': account.x_studio_advance_payment_account_purchases_1.id})
                        record.write({'x_studio_advance_payment_acc_updated': True})
                    else:
                        raise UserError('There is no payment line to update.')
            else:
                raise UserError('There is no payment to update.')
    else:
        raise UserError('Advance Payment Accounts have not been setup in Accounting Configuration')
]]></field>
    </record>

    <!-- 1310: Rates. Window action opening x_custom_currency_rate
         filtered by the parent currency. Referenced from
         views/x_custom_currency_studio_ported_v2.xml line 43. -->
    <record id="act_custom_currency_rates" model="ir.actions.act_window">
        <field name="name">Rates</field>
        <field name="res_model">x_custom_currency_rate</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('x_studio_custom_currency_id', '=', active_id)]</field>
        <field name="context">{'search_default_x_studio_custom_currency_id': active_id, 'default_x_studio_custom_currency_id': active_id}</field>
        <field name="binding_view_types">list,form</field>
    </record>

    <!-- 1676: RPT - Sample Button. Verbatim from Clear-DB — the code
         is only comments (a no-op template). Button is invisible=1 on
         the view arch anyway, so this record exists purely to resolve
         the xmlid. Referenced from
         views/x_sales_report_model_studio_ported_v2.xml line 25. -->
    <record id="srv_sales_report_sample_button" model="ir.actions.server">
        <field name="name">RPT - Sample Button</field>
        <field name="model_id" ref="model_x_sales_report_model"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code">action = None  # No-op template action from Clear-DB (Studio scaffold).</field>
    </record>

    <!-- 1386: Vendor Bill. Window action opening account.move filtered
         by parent TP invoice header. Referenced from
         views/x_tp_invoice_header_studio_ported_v2.xml line 46 as a
         stat button. -->
    <record id="act_tp_invoice_vendor_bill" model="ir.actions.act_window">
        <field name="name">Vendor Bill</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('x_studio_tp_id', '=', active_id)]</field>
        <field name="context">{'search_default_x_studio_tp_id': active_id, 'default_x_studio_tp_id': active_id}</field>
        <field name="binding_view_types">list,form</field>
    </record>

    <!-- 1384: IMP - Post TP Invoice. Real workflow action - 8 KB.
         Creates a TP-reversal account.move with cost-allocation lines
         (by weight / by volume / by amount) based on
         x_imports_ledger_setup config. Ported verbatim from Clear-DB.
         Restored to the primary form header in v0.0.29 (auto-porter
         had stripped it). Depends on x_consignment_header +
         x_imports_ledger_setup + stock.valuation.layer - if any are
         missing on-disk, the action fails at click-time only. -->
    <record id="srv_tp_invoice_post" model="ir.actions.server">
        <field name="name">IMP - Post TP Invoice</field>
        <field name="model_id" ref="model_x_tp_invoice_header"/>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.id:
    company_id = env.context.get('allowed_company_ids', [env.user.company_id.id])[0]
    company = env['res.company'].browse(company_id)

    invalid_lines = env['x_tp_invoice_line'].search([('x_studio_tp_invoice_header_id', '=', record.id)])
    diff = 0
    diff_found = False
    if invalid_lines:
        for val in invalid_lines:
            if val.x_studio_invoice_amount == 0.0000:
                raise UserError("Invoice Amount of Individual TP Lines Should be Greater than Zero!")
            if val.x_studio_invoice_amount != val.x_studio_original_amount:
                diff_found = True
                diff += (val.x_studio_invoice_amount - val.x_studio_original_amount)

    if record.x_studio_vendor.id == 0:
        raise UserError("Vendor Account must be Specified!")
    if record.x_studio_currency_id.id == 0:
        raise UserError("Currency must be Specified!")
    if record.x_studio_invoice_no == False:
        raise UserError("Invoice No must be Specified!")
    if record.x_studio_invoice_date == False:
        raise UserError("Invoice Date must be Specified!")

    tax_account = env['x_imports_ledger_setup'].search([('x_studio_company_id', '=', company.id)], limit=1)
    if tax_account:
        if tax_account.x_studio_tp_invoicing_tax_acc.id == 0:
            raise UserError('TP Invoicing Tax Account must be Specified in Imports Ledger Setup')
        if tax_account.x_studio_import_tp_vendor_account.id == 0:
            raise UserError('Import TP Vendor Account must be Specified in Imports Ledger Setup')
    else:
        raise UserError('Imports Ledger Setup must be specified for the selected company ')

    tp_lines = []

    if diff_found == True:
        qty = 0
        con_lines = env['x_consignment_header'].search([('id', '=', record.x_studio_con_no.id)])
        if con_lines:
            qty = 1 if diff > 0 else -1

            if tax_account.x_studio_cost_allocation_method == 'by_weight':
                for diff_lines in con_lines.x_studio_consignment_line_ids:
                    tp_lines.append([0, 0, {
                        'product_id': diff_lines.x_studio_product_id.id,
                        'quantity': qty,
                        'currency_id': record.x_studio_currency_id.id,
                        'name': diff_lines.x_studio_product_id.name,
                        'product_uom_id': diff_lines.x_studio_product_id.uom_id.id,
                        'price_unit': abs(diff) * (diff_lines.x_studio_weight / con_lines.x_studio_total_amount_cost_allocation_method),
                        'price_subtotal': (abs(diff) * (diff_lines.x_studio_weight / con_lines.x_studio_total_amount_cost_allocation_method)) * qty,
                        'account_id': diff_lines.x_studio_product_id.categ_id.property_stock_account_input_categ_id.id,
                    }])
                    value_entry = env['stock.valuation.layer'].search([('stock_move_id', '=', diff_lines.x_studio_stock_move_id.id)], limit=1)
                    if value_entry:
                        adj = (abs(diff) * (diff_lines.x_studio_weight / con_lines.x_studio_total_amount_cost_allocation_method)) * qty
                        value_entry.write({
                            'value': value_entry.value + adj,
                            'unit_cost': (value_entry.value + adj) / value_entry.quantity,
                            'remaining_value': (value_entry.value + adj) / value_entry.remaining_qty,
                        })
            elif tax_account.x_studio_cost_allocation_method == 'by_volume':
                for diff_lines in con_lines.x_studio_consignment_line_ids:
                    tp_lines.append([0, 0, {
                        'product_id': diff_lines.x_studio_product_id.id,
                        'quantity': qty,
                        'currency_id': record.x_studio_currency_id.id,
                        'name': diff_lines.x_studio_product_id.name,
                        'product_uom_id': diff_lines.x_studio_product_id.uom_id.id,
                        'price_unit': abs(diff) * (diff_lines.x_studio_volume / con_lines.x_studio_total_amount_cost_allocation_method),
                        'price_subtotal': (abs(diff) * (diff_lines.x_studio_volume / con_lines.x_studio_total_amount_cost_allocation_method)) * qty,
                        'account_id': diff_lines.x_studio_product_id.categ_id.property_stock_account_input_categ_id.id,
                    }])
                    value_entry = env['stock.valuation.layer'].search([('stock_move_id', '=', diff_lines.x_studio_stock_move_id.id)], limit=1)
                    if value_entry:
                        adj = (abs(diff) * (diff_lines.x_studio_volume / con_lines.x_studio_total_amount_cost_allocation_method)) * qty
                        value_entry.write({
                            'value': value_entry.value + adj,
                            'unit_cost': (value_entry.value + adj) / value_entry.quantity,
                            'remaining_value': (value_entry.value + adj) / value_entry.remaining_qty,
                        })
            else:
                for diff_lines in con_lines.x_studio_consignment_line_ids:
                    tp_lines.append([0, 0, {
                        'product_id': diff_lines.x_studio_product_id.id,
                        'quantity': qty,
                        'currency_id': record.x_studio_currency_id.id,
                        'name': diff_lines.x_studio_product_id.name,
                        'product_uom_id': diff_lines.x_studio_product_id.uom_id.id,
                        'price_unit': abs(diff) * (diff_lines.x_studio_amount / con_lines.x_studio_total_amount),
                        'price_subtotal': (abs(diff) * (diff_lines.x_studio_amount / con_lines.x_studio_total_amount)) * qty,
                        'account_id': diff_lines.x_studio_product_id.categ_id.property_stock_account_input_categ_id.id,
                    }])
                    value_entry = env['stock.valuation.layer'].search([('stock_move_id', '=', diff_lines.x_studio_stock_move_id.id)], limit=1)
                    if value_entry:
                        adj = (abs(diff) * (diff_lines.x_studio_amount / con_lines.x_studio_total_amount)) * qty
                        value_entry.write({
                            'value': value_entry.value + adj,
                            'unit_cost': (value_entry.value + adj) / value_entry.quantity,
                            'remaining_value': (value_entry.value + adj) / value_entry.remaining_qty,
                        })

    tp_lines.append([0, 0, {
        'account_id': tax_account.x_studio_import_tp_vendor_account.id,
        'name': 'Import TP Invoice - Reversal',
        'quantity': 1.00,
        'currency_id': record.x_studio_currency_id.id,
        'price_unit': record.x_studio_total_original_invoice_amount,
    }])

    tp_lines.append([0, 0, {
        'account_id': tax_account.x_studio_tp_invoicing_tax_acc.id,
        'name': 'Total Tax',
        'quantity': 1.00,
        'currency_id': record.x_studio_currency_id.id,
        'price_unit': record.x_studio_taxes_1,
    }])

    journal = env['account.journal'].search([('name', '=', 'TP Vendor Bills'), ('company_id', '=', company.id)], limit=1)
    if not journal:
        raise UserError('The required journal has not been setup. Process terminated.')

    tp_reversal = env['account.move'].create({
        'x_studio_tp_id': record.id,
        'partner_id': record.x_studio_vendor.id,
        'journal_id': journal.id,
        'move_type': 'in_invoice',
        'currency_id': record.x_studio_currency_id.id,
        'x_studio_supplier_invoice_number': record.x_studio_invoice_no,
        'invoice_date': record.x_studio_invoice_date,
        'invoice_line_ids': tp_lines,
    })

    update_tp_reversal = env['account.move'].search([('id', '=', tp_reversal.id)], limit=1)
    if update_tp_reversal:
        update_tp_reversal.write({'state': 'posted'})

    record['x_studio_status'] = 'Posted'
    record['x_studio_pipeline_status_bar'] = 'Posted'

    action = {
        'name': 'Journal Entries',
        'domain': [('id', '=', tp_reversal.id)],
        'type': 'ir.actions.act_window',
        'res_model': 'account.move',
        'view_mode': 'tree,form',
        'view_id': False,
        'context': False,
    }
]]></field>
    </record>

    <!-- 1404: IMP - Post LC. Sets x_studio_status/x_studio_selection_field_yo4qM
         to 'Posted' after validating LC amount. Referenced from the primary
         form view (restored in v0.0.26; auto-porter had dropped it). -->
    <record id="srv_lc_post_lc" model="ir.actions.server">
        <field name="name">IMP - Post LC</field>
        <field name="model_id" ref="model_x_lc_header"/>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.x_studio_lc_amount == 0.00:
    raise UserError('LC Amount must be Specified')

record['x_studio_status'] = 'Posted'
record['x_studio_selection_field_yo4qM'] = 'Posted'
]]></field>
    </record>

    <!-- 1405: IMP - Amend LC. Clones the LC to a new record with
         incremented version_no, then flips the source status to 'Amended'.
         Referenced from the primary form view (restored in v0.0.26). -->
    <record id="srv_lc_amend_lc" model="ir.actions.server">
        <field name="name">IMP - Amend LC</field>
        <field name="model_id" ref="model_x_lc_header"/>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
if record.id:
    lc = env['x_lc_header'].create({'x_studio_created_from_purchase_order': record.x_studio_created_from_purchase_order.id})
    lc.write({
        'x_name': record.x_name,
        'x_studio_indent_no': record.x_studio_indent_no,
        'x_studio_additional_conditions': record.x_studio_additional_conditions,
        'x_studio_available_with_by': record.x_studio_available_with_by,
        'x_studio_bank_charges': record.x_studio_bank_charges,
        'x_studio_beneficiary': record.x_studio_beneficiary,
        'x_studio_confirmation_instructions': record.x_studio_confirmation_instructions,
        'x_studio_currency_id': record.x_studio_currency_id,
        'x_studio_date_of_expiry': record.x_studio_date_of_expiry,
        'x_studio_date_of_issue': record.x_studio_date_of_issue,
        'x_studio_date_of_receipt': record.x_studio_date_of_receipt,
        'x_studio_deferred_days': record.x_studio_deferred_days,
        'x_studio_description_of_goods': record.x_studio_description_of_goods,
        'x_studio_document_required': record.x_studio_document_required,
        'x_studio_draft': record.x_studio_draft,
        'x_studio_instruction_remarks': record.x_studio_instruction_remarks,
        'x_studio_issuing_bank': record.x_studio_issuing_bank.id,
        'x_studio_issuing_bank_reference_no_1': record.x_studio_issuing_bank_reference_no_1,
        'x_studio_latest_shipment_date': record.x_studio_latest_shipment_date,
        'x_studio_lc_amount': record.x_studio_lc_amount,
        'x_studio_lc_credit_sub_type': record.x_studio_lc_credit_sub_type,
        'x_studio_lc_credit_type': record.x_studio_lc_credit_type,
        'x_studio_lc_no': record.x_studio_lc_no,
        'x_studio_loading_on_board': record.x_studio_loading_on_board,
        'x_studio_partial_shipment': record.x_studio_partial_shipment,
        'x_studio_period_for_presentation': record.x_studio_period_for_presentation,
        'x_studio_place_of_expiry': record.x_studio_place_of_expiry,
        'x_studio_revised_lc_amount': record.x_studio_revised_lc_amount,
        'x_studio_tolerance': record.x_studio_tolerance,
        'x_studio_tolerance_type': record.x_studio_tolerance_type,
        'x_studio_transportation': record.x_studio_transportation,
        'x_studio_transshipment': record.x_studio_transshipment,
        'x_studio_vendor': record.x_studio_vendor.id,
        'x_studio_version_no': (record.x_studio_version_no + 1),
    })

    action = {
        'name': 'Letter of Credit',
        'domain': [('id', '=', lc.id)],
        'type': 'ir.actions.act_window',
        'res_model': 'x_lc_header',
        'view_mode': 'tree,form',
        'view_id': False,
        'context': False,
    }

    record['x_studio_status'] = 'Amended'
    record['x_studio_selection_field_yo4qM'] = 'Amended'
]]></field>
    </record>

    <!-- 1378: IMP - Apply Selected Charge Lines to TP Invoice.
         Full port of the Clear-DB Python — real workflow action
         that creates an x_tp_invoice_header from selected consignment
         charge lines on the temp header. Referenced from
         views/x_temp_tp_invoice_head_studio_ported_v2.xml line 47.
         Depends on x_consignment_header (model may not be on-disk yet
         — the action will fail at click-time if so, but install
         completes). -->
    <record id="srv_tp_invoice_apply_charges" model="ir.actions.server">
        <field name="name">IMP - Apply Selected Charge Lines to TP Invoice</field>
        <field name="model_id" ref="model_x_temp_tp_invoice_head"/>
        <field name="state">code</field>
        <field name="usage">ir_actions_server</field>
        <field name="sequence">5</field>
        <field name="code"><![CDATA[
lines = []
select = 0

for valid_line in record.x_studio_charge_line:
    if valid_line.x_studio_select == True:
        select += 1
        lines.append([0, 0, {
            'x_studio_consignment_charge_header_id': valid_line.x_studio_consignment_charge_header_id.id,
            'x_studio_consignment_id': valid_line.x_studio_consignment_id.id,
            'x_studio_charge_group': valid_line.x_studio_charge_group,
            'x_studio_charge_name': valid_line.x_studio_charge_name,
            'x_studio_basis': valid_line.x_studio_basis,
            'x_studio_original_amount': valid_line.x_studio_amount,
            'x_studio_invoice_amount': valid_line.x_studio_amount,
        }])

        charge_line = env['x_consignment_charge_h'].search([('id', '=', valid_line.x_studio_consignment_charge_header_id.id)])
        if charge_line:
            charge_line.write({'x_studio_tp_processed': True})

if select == 0:
    raise UserError("Atleast One Line Should be Selected to Proceed.")

con_header = env['x_consignment_header'].search([('id', '=', record.x_studio_consignment_header_id.id)], limit=1)

if con_header:
    tp_invoice = env['x_tp_invoice_header'].create({
        'x_studio_con_no': record.x_studio_consignment_header_id.id,
        'x_studio_vendor': record.x_studio_supplier_id.id,
        'x_studio_currency_id': record.x_studio_supplier_id.property_purchase_currency_id.id,
        'x_studio_invoice_no': con_header.x_studio_supplier_invoice_number,
        'x_studio_invoice_date': con_header.x_studio_invoice_date,
        'x_studio_tp_lines': lines,
    })
else:
    tp_invoice = env['x_tp_invoice_header'].create({
        'x_studio_con_no': record.x_studio_consignment_header_id.id,
        'x_studio_vendor': record.x_studio_supplier_id.id,
        'x_studio_currency_id': record.x_studio_supplier_id.property_purchase_currency_id.id,
        'x_studio_tp_lines': lines,
    })

if con_header:
    con_header.write({'x_studio_status': 'TP Invoice', 'x_studio_status_bar': 'TP Invoice'})

if record.id:
    temp_rec = env['x_temp_tp_invoice_head'].search([('id', '=', record.id)], limit=1)
    temp_rec.unlink()

action = {
    'name': 'TP Invoice',
    'domain': [('id', '=', tp_invoice.id)],
    'type': 'ir.actions.act_window',
    'res_model': 'x_tp_invoice_header',
    'view_mode': 'tree,form',
    'view_id': False,
    'context': False,
}
]]></field>
    </record>

</odoo>
